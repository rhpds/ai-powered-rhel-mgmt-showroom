---
# Module 10 — Solve playbook
# Automates all student tasks for this module

- name: Solve Module 10
  hosts: bastion
  gather_facts: false
  become: true
  vars:
    ansible_user: "{{ bastion_user | default('lab-user') }}"
  tasks:
    - name: Placeholder — replace with actual solve tasks
      ansible.builtin.debug:
        msg: "Solve logic goes here"
