---
# Module 07 — Setup playbook (optional)
# Runs once when module is first loaded

- name: Setup Module 07
  hosts: bastion
  gather_facts: false
  become: true
  vars:
    ansible_user: "{{ bastion_user | default('lab-user') }}"
  tasks:
    - name: No setup needed
      ansible.builtin.debug:
        msg: "Setup complete"
